@model IEnumerable<Product>
@{
    ViewData["Title"] = "Product ⇄ NG Mapping";
}

<div class="row mb-3">
    <div class="col-12 d-flex justify-content-between align-items-center">
        <div>
            <h2 class="mb-0"><i class="fa-solid fa-ban me-2"></i>Product ⇄ NG Mapping</h2>
            <div class="text-secondary small">Atur tipe NG yang relevan untuk setiap produk</div>
        </div>
        <a asp-action="Products" class="btn btn-outline-secondary">
            <i class="fa-solid fa-arrow-left me-1"></i>Back to Products
        </a>
    </div>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="fa-solid fa-check-circle me-2"></i>@TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-dark table-hover align-middle mb-0">
                <thead>
                    <tr>
                        <th style="width: 25%;">Product</th>
                        <th>Material Code</th>
                        <th>Mapped NG Types</th>
                        <th style="width: 12%;"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var p in Model)
                    {
                        var ngTypes = p.ProductNgTypes?.Select(pn => $"{pn.NgType?.Code} - {pn.NgType?.Name}").ToList() ?? new List<string>();
                        <tr>
                            <td>@p.Name</td>
                            <td>@p.MaterialCode</td>
                            <td>
                                @if (ngTypes.Any())
                                {
                                    <span>@string.Join(", ", ngTypes)</span>
                                }
                                else
                                {
                                    <span class="text-secondary fst-italic">Belum ada NG Type yang di-assign</span>
                                }
                            </td>
                            <td class="text-end">
                                <a asp-action="EditProductNgTypes" asp-route-id="@p.Id" class="btn btn-sm btn-danger">
                                    <i class="fa-solid fa-pen-to-square me-1"></i>Edit
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

